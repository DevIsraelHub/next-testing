export interface WebsiteType {
    id: number,
    domain: string | null,
    last_edited_ago: string,
    preview_url: string,
    slug: string | null,
    subdomain: string | null,
    title: string
}